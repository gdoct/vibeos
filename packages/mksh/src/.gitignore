# Build products and the cached distfile for the mksh port (regenerated by
# `make` / `pkg create`). Only the Makefile is tracked. The distfile (*.tgz) is
# also excluded by the repo's top-level .gitignore; listed here for clarity.
/mksh-*.tgz
/mksh/
/build/
