# Build products and the cached distfile for the toybox port (regenerated by
# `make` / `pkg create`). Only the Makefile is tracked; the distfile and the
# extracted/built trees are not committed.
/toybox-*.tar.gz
/toybox-*/
/build/
