# Memory index

- [Build/run image gotcha](build-run-image-gotcha.md) — `make image` (not just `make`) refreshes the bootable `vibeos.img`; bare `make` can skip the relink.
- [SMP userspace multicore](smp-userspace-multicore.md) — user tasks now run on all cores; what was hardened and how it's verified.
